# RAG Module

## Responsibility
Serves as the central orchestrator that guides the flow of a user query through permission filtering, retrieval, reranking, graph context augmentation, LLM prompt assembly, and synthesis of final answers.
